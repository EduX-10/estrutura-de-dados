#include <stdio.h>
#include <stdlib.h>

struct node {
    int info;
    struct node *next;
};
typedef struct node *NODEPTR;

NODEPTR getnode(){
    NODEPTR p;
    p = (NODEPTR) malloc(sizeof(struct node));
    return(p);
}
//p = (NODEPTR) malloc(sizeof(struct node));


void freenode(){
    NODEPTR p;
    free(p);
}
//free(p);

int main(){
    
    return 0;
}
